<?php
// Set the session cookie to expire in 1 hour
// session_set_cookie_params(3600, '/', '', true, true);

// Set the session cookie manually, if necessary (normally not needed)
// setcookie('PHPSESSID', session_id(), time() + 3600, '/', '', false, true);

// Start the session
session_start(); //PHP_SESSION_ACTIVE
